//for enviroment variables
export let API = `http://localhost:5000`;
export let API_URL = `${API}/api/v1`;

// if (process.env.NODE_ENV === "development") {
//   API = `http://localhost:5000`;
// } else {
//   API = `http://192.168.9.241:5001`;
// }
